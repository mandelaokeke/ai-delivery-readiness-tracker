import { BookOpen, Mail, MessageCircleQuestion, Users } from "lucide-react";
import { AppShell } from "@/components/AppShell";
import { getAppContext } from "@/lib/app-context";
export const dynamic = "force-dynamic";
export default async function HelpPage() { const c = await getAppContext(); return <AppShell active="help" organisation={c.organisation} userName={c.userName} userEmail={c.userEmail} configured={c.configured} workstreamCount={c.workstreams.length}><section className="page-heading"><div><span className="eyebrow">Support</span><h1>Help centre</h1><p>Quick answers for running your delivery workspace.</p></div><a className="primary-action" href="mailto:support@mandai.app"><Mail size={17} /> Contact support</a></section><section className="help-grid"><article><BookOpen size={22} /><h2>Create a workstream</h2><p>Open Workstreams, select New workstream, and add an owner, milestone, risk, health, and due date.</p></article><article><MessageCircleQuestion size={22} /><h2>Ask MANDAI</h2><p>Use the assistant on Overview to summarize risks, identify priorities, or prepare a leadership update from current workstreams.</p></article><article><Users size={22} /><h2>Invite teammates</h2><p>Open Team and use Invite by email to share the secure signup link with collaborators.</p></article></section></AppShell>; }
